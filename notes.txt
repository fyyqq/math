from IPython.display import display, Math
import sympy

display() = print as math symbols
display(Math(f"")) = math action inside

display(Math("\\frac")) = divide symbol
display(Math("\\times")) = times symbol
display(Math("5^2")) = power symbol

x, y, z = sympy.symbols('x y z') = create a math symbols 
sympy.latex() = 
sympy.simplify() = 
sympy.solve() = 

math = x + 5
math.subs({ x : 2}) = change symbol 'x' to 2s

====== MATH ======
mean = find middle data
median = sort & find middle data
modus = find most duplicated data
std = ---------------------
25% = 25% front data
50% = 50% front data
75% = 75% front data
abs = negative number to positive
mode = get duplicated number
min = minimum data
max = maximum data
count = length of data
sum = addition data
prod = multiple data
cumsum = addition data from left / top
cumprod = multiple data from left / top